export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum Role {
  ADMIN = 'ADMIN',
  TA = 'TA',
  GUEST = 'GUEST',
}

export enum PaymentStatus {
  SAVED = 'SAVED',
  SENT = 'SENT',
  PAID = 'PAID',
}
